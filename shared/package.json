{
  "name": "@gemini-video-platform/shared",
  "version": "1.0.0",
  "description": "Shared types and constants for Gemini Video Platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p ./tsconfig.json --pretty false",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist/**/*"
  ],
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
