# Copy this file to .env in the root directory and update the values
# Backend Environment Variables
PORT=3001
NODE_ENV=development
LOG_LEVEL=info

# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Database Configuration
# For Railway/Render: Use /data/database.db for persistent volume
DATABASE_PATH=./database.db

# Frontend Configuration
# Update with your production domain for CORS
FRONTEND_URL=http://localhost:3000

# Video Storage Configuration
VIDEO_STORAGE_TYPE=local
# For Railway/Render: Use /data/videos for persistent volume
VIDEO_STORAGE_PATH=./data/videos

# JWT Configuration
# IMPORTANT: Generate strong random secrets for production!
# Use: openssl rand -base64 32
JWT_SECRET=your_super_secure_jwt_secret_key_here
JWT_REFRESH_SECRET=your_super_secure_jwt_refresh_secret_key_here

# Firebase Configuration (optional, only required if VIDEO_STORAGE_TYPE=firebase)
# To set up Firebase Storage:
# 1. Create a Firebase project at https://console.firebase.google.com
# 2. Enable Cloud Storage in your Firebase project
# 3. Create a service account at https://console.cloud.google.com/iam-admin/serviceaccounts
# 4. Download the service account JSON and extract the values below
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour_Private_Key_Here\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=your_service_account_email@your_project.iam.gserviceaccount.com

# Production Environment Variables (set these when deploying)
# NODE_ENV=production
# PORT=3001 (Railway/Render set this automatically)
# DATABASE_PATH=/data/database.db (persistent volume)
# VIDEO_STORAGE_PATH=/data/videos (persistent volume)
# FRONTEND_URL=https://your-frontend-domain.com
# LOG_LEVEL=info (or warn/error for production)