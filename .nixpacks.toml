[phases.setup]
nixPkgs = ["nodejs-18_x", "python3"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build:shared", "npm run build:backend"]

[start]
cmd = "cd backend && npm start"
